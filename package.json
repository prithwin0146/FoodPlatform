{
  "name": "seetheprep-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "npm --prefix client start",
    "test": "npm --prefix client test"
  }
}
